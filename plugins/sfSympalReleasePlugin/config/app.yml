all:
  sympal_config:
    content_types:
      CoreRelease:
        content_templates:
          default_view:
            template:      core_release/view
        content_slots:
          archive_filename:
            edit_mode:     inline
            type:          Text
          details:
            edit_mode:     inline
            type:          Markdown
