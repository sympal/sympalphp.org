all:
  sympal_config:
    content_types:
      CoreRelease:
        content_templates:
          default_view:
            template:      core_release/view
        content_slots:
          title:
            edit_mode: inline
          archive_filename:
            edit_mode: inline
            template:  core_release/download_button
          details:
            type:      Markdown
            edit_mode: inline
