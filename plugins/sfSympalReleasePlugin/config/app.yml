all:
  sympal_config:
    content_types:
      CoreRelease:
        content_templates:
          default_view:
            template:      core_release/view
        content_slots:
          details:
            type:     Markdown
